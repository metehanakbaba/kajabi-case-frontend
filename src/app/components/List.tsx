import React from "react"
import styled from "styled-components"

const List: React.FC<React.PropsWithChildren> = styled.div`
  background: white;
  border-radius: 2px;
  list-style: none;
  padding: 10px 20px;
`;

export default List;
